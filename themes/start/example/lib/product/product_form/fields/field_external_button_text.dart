import 'package:flutter/material.dart';
import 'package:ui/ui.dart';

class FieldExternalButtonText extends StatelessWidget {
  const FieldExternalButtonText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const InputTextField(label: 'Button Text');
  }
}
