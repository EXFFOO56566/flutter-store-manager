import 'package:flutter/material.dart';
import 'package:ui/ui.dart';

class FieldRegularPrice extends StatelessWidget {
  const FieldRegularPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const InputTextField(label: 'Regular Price');
  }
}
